# 质量改进验证记录（2026-09-25）

## Release worker 生命周期

设备：Apple M5 Pro，64 GiB；macOS 27.0（26A428）。使用本机已安装 Qwen3-ASR 0.6B 8-bit。
3 条合成 fixture（英文、中文、静音），每种条件各重复 3 次。未使用麦克风、未自动下载模型、未写入历史或输出到其他应用。

以下为 **worker 请求**的 P50/P95，单位 ms；每行 n=9，含不同长度的 3 条样本，不代表 Fn 到粘贴延迟。峰值为进程生命周期最大 RSS。

| 条件 | 成功 | P50 | P95 | 最大 RSS MiB |
| --- | ---: | ---: | ---: | ---: |
| cold-model | 9/9 | 916.2 | 1013.5 | 1397.0 |
| cold-process | 9/9 | 987.7 | 4335.4 | 1108.6 |
| first-inference | 9/9 | 110.7 | 137.8 | 1391.7 |
| idle-released | 9/9 | 901.9 | 927.2 | 1398.0 |
| idle-retained | 9/9 | 110.0 | 143.4 | 1108.9 |
| preview-agent | 9/9 | 116.8 | 134.5 | 1212.5 |
| preview-realtime | 9/9 | 115.7 | 141.8 | 1212.9 |
| preview-subtitle | 9/9 | 101.0 | 133.6 | 1212.3 |
| warm | 9/9 | 91.2 | 103.6 | 1108.7 |

81 次计分请求全部成功；27 次最终离线静音结果均为空。首个冷进程请求较慢，仍保留在统计中。
idle 条件只测试 5 秒闲置后的 worker 驻留/公开 release 请求，不是 App 自动闲置策略验收。
预览候选可能包含控制标记；本轮原始文件中的 first_preview_ms/stable_preview_ms 仅表示原始 worker 事件。采集器已更名以避免与宿主可展示字幕混淆。

源提交：`8a1dce29d0fabea7b72a03b5e442382109f9c5c5` 的工作中快照。
源指纹：`fb679fea8120660562f88fe8f60325316b7a994abaa9ba81f1fd3ef7a399bdad`。
完整私有原始结果、fixture、构建收据和内容无关摘要保存在本地 `.artifacts/asr-lifecycle-2026-09-25/`，不提交音频与正文。

这组数据只证明该候选在这些条件下完成 Release 模型链路；没有基线配对、120 条真人录音或人工编辑成本证据，未据此启用新的预热、驻留、缓存或 profile 默认值。

## 本地与原生检查

- 合并主线前的三轮 `just ci` 均通过；系统边界及回放切片也通过定向测试与 `prek`。
- 主线 `0d1cda4` 整合后 App 构建、完整模块边界、定向回归和 `just ci` 均通过。门禁包含 Release App/worker/IME、临时签名、打包输入法检查及全量测试。
- `just test-render` 通过；已检查纠错弹窗、快捷面板、设置及工作流浅深色图片。
- 导出图中的原生侧栏选中行出现黑块，尚需区分捕获限制与真实窗口问题。
- Fn、真实麦克风、IME、外部粘贴竞争、VoiceOver、高对比度、减少动态效果、低内存设备、最低支持系统及安装包信任均未在本轮完成物理验收。
- 下方补充了隔离输出的 Release 宿主路径测量；真实端到端粘贴、120 条获授权语料和质量非退化验收仍待完成。

临时音频边界切片：删除、隔离和带重试的清理移到 Platform，Runtime 接收必需接口，
Core 保留值校验与协议。录音、延迟录音、失败恢复、超时和 TTS 定向测试通过；
新增真实文件测试验证源文件删除后隔离副本仍可读取、拒绝符号链接且不改变调用方文件。
模块图检查通过，独立只读审查未发现本轮可达回归。测试默认组装位于独立领域测试支持目标。

设置状态切片：界面语言、语音模型、驻留选择、输出偏好和 LLM 配置等 17 个值
移入设置功能模型；视图直接读取功能状态，原 AppModel 属性已删除。AppModel、设置、
语音就绪和字幕相关测试通过，原加载/用户修改/退出竞争行为保持。

整合回归补齐了：模型身份单一来源、词库快照冻结、Jev 准备结果经录音处理队列传递，
以及失败出口取消准备任务。路由规则变更集现在记录规则新增/修改/删除，
删除记录集后的停用路由能从 SQLite 重载；缺少目标的路由仍禁止重新启用。


## 归档与测量链路

评测归档已接入选择导出界面，中英文、浅深色原生渲染通过；未导出任何用户录音。
真实加密归档 fixture 验证了选中项限定、认证、权限、SHA256、取消清理和清理失败提示。
关闭再打开清除本次授权，等待旧任务退出后重载；不把未标注的录音标记为静音。
诊断事件生产端改为固定枚举，旧 JSON/SQLite 字符串边界继续兼容并清洗未知事件。
历史周期维护测试使用显式 tick，设置验证与工作流解释共用保留取消任务的所有者，退出取消并排空。

以下 Release 宿主回放使用生产识别、词汇处理、空白规范化、加密 Record 提交及隔离内存输出端。
3 条合成录音各重复 3 次，两个条件共 18 次计分请求全部成功；12 次有语音均保存后派发，
6 次静音均没有 Record 提交或输出。另保留 1 次未计分热身。没有调用麦克风、云服务或真实粘贴。

| 条件 | 指标 | n | P50 ms | P95 ms |
| --- | --- | ---: | ---: | ---: |
| warm | 宿主准入至最终 ASR | 9 | 95.5 | 184.1 |
| warm | 宿主准入至保存 | 6 | 101.6 | 110.2 |
| warm | 宿主准入至隔离输出派发 | 6 | 101.6 | 110.3 |
| first_inference | 宿主准入至最终 ASR | 9 | 131.8 | 159.9 |
| first_inference | 宿主准入至保存 | 6 | 137.4 | 163.3 |
| first_inference | 宿主准入至隔离输出派发 | 6 | 137.4 | 163.3 |

三个指标的样本集合不同，不能用分位数相减推断阶段耗时；缺失的保存/派发不补零。
模型加载在 first_inference 计时前单独完成；不是包含模型加载的冷启动测量。
源快照：`de5a213` 后的工作中版本，指纹
`fb1d68960621b927015289ca82f4f677660293aad3ef40756c7580512f062386`。
warm run：`0d82d390-a396-4aca-9157-4e32ed1f9a4a`；first run：`647c9d29-105b-45b0-bed3-555a39d9b3b2`。
报告与内容无关汇总保存在 `.artifacts/asr-host-2026-09-25/`，原始正文不提交。
源码、worker 产物和实际模型 revision 均经校验。该快照之后的 UI 退出、语音资源所有权、词库与查询命令变化不包含在此指纹内。

第一次使用完整 Release 测试图时，`SmartCleanupRenderTests` 遇到 SwiftUI opaque tag 符号链接失败，
没有采样，报告保留为 invalid。宿主基准改用同一 manifest 的 Release 领域目标；
完整生产 Release App/worker/IME 构建通过，完整 CI 的全量测试范围不缩小。
此证据没有真人质量配对、总峰值内存、Fn 和目标应用实际显示端点，不据此启用新默认策略。

本切片完整 `just ci` 已通过（包含归档、类型化诊断、功能命令迁移和退出排空回归）。
原生渲染仅验证静态布局；上述物理交互和正式安装信任仍未验收。


## 功能命令与任务所有权回归

在 `1c99451` 之后继续迁移唤醒词与 ASR 准备任务、词库编辑，以及 Record 查询命令。
语音生命周期定向测试覆盖替换/取消后晚到、模型信任检查、准备进度与终止退出；
新增屏障测试验证切换模型后旧唤醒词准备结果不能发布，退出后不能再准入。
词库回归覆盖作用域保存/重载、损坏设置恢复、输入法显式记词和加载/不可用/退出保护；
删除旧 AppModel 转发和可写规则投影。
Record 回归覆盖工作区/快捷面板跨页精确匹配优先、近似匹配分页、筛选重置、预览选择、
Jev 返回和历史导航；工作区已停止使用裸 offset 合并不同 revision 的查询。
这些是源码与自动化回归证据，尚不代表 Fn、麦克风、焦点或辅助功能真机验收。


## 隐私、字幕与设置呈现回归

`24d3b09` 后将隐私值及保存状态归入设置模型，共用按键写入协调器；未开始的旧快照
合并为最新完整快照，已开始的存储调用仍需排空。字幕节流/隐藏归入语音模型，
活动保留期限删除 AppModel 的平行状态。隐私保存、失败重试、加载/退出和历史保留
定向回归通过。可控计时器验证取消隐藏不会擦除同一运行的新字幕；退出立即关闭浮层，
晚到计时器、字幕和队列刷新不能重开。独立只读审查发现并已修复隐私读取遗漏及退出浮层残留。
首次设置只列当前阻塞项；可选 LLM 不影响本机语音就绪。工作流先展示用途、触发和输出，
处理步骤折叠。Record 输出端改为不可变注册表，重复身份返回明确错误。


上述隐私/字幕/呈现与重复注册切片的完整 `just ci`、`prek` 和原生渲染通过，
已拆为 `f2a16ce` / `82d2847`。渲染中的原生侧栏捕获异常仍按前述边界待真机核验。
后续构造依赖回归删除 Record 交互默认空实现和二次安装，验证初始关闭、设置恢复、
用户修改覆盖旧快照、单调 revision 和显式复制返回真实结果；不改变系统原生复制/粘贴。
