# UI 精修验收记录

## 基线

- HEAD：`2eda5e163307aab0e4c92c7c9f9a7b7f3503f2f4`。
- 开始时存在 37 个修改文件；差异与 revision 保存在 `/tmp/rill-ui-baseline-20260912/`。
- 原有修改分类：共享样式／符号；页面与浮窗布局；本地化；焦点／搜索；已有 UI 测试。本轮在这些改动上继续实现，没有撤销或重新生成无关文件。
- 已安装应用报告相同 HEAD，但 dirty revision 不能证明二进制包含完全相同的未提交源码；其截图只作为观察基线。
- 原生基线截图位于上述目录的 `screenshots/`。截图包含个人历史，仅保留本地，不加入 Git。

## 实现与验证边界

- 内置工作流表单按计划设为只读；保留已有覆盖的恢复默认值入口与底层配置能力。
- 回执只包含动作结果和耗时；历史展示真实结果，不补造目标应用名称。
- 草稿保护覆盖 AppModel 导航、列表选择、重置和重新加载。退出应用沿用现有生命周期；不以 UI 重构改变关闭协议。
- 原生测试服务截图与真实应用截图分开记录。任何渲染缺失、设备交互未覆盖项都不能计为通过。

## 验证进度

- 首轮既有 RillUITests：535 项，通过。
- 新增草稿导航、布局边界、目标文案及删除后选择测试：通过。
- 原生测试截图首轮透明背景已修正；第二轮导出 40 张中英文、深浅色、960/1280pt 管理界面图。内容区可辅助检查，AppKit 侧栏选择态仍不完整，未作为完整视觉验收证据。
- `just ci` 通过：1714 项测试，0 失败，1 项按默认配置跳过的截图导出测试；其中 UI 测试 544 项。
- 最终源码 `just check`（prek）及 Gitleaks 扫描通过。
- CI 首次遇到资源检查仍使用旧中间产物路径，已从实际 Release 输出目录推导路径，并同步发布策略检查；干净 Release、资源检查、打包签名烟雾检查随后通过。

## 实际应用第一轮

- QA 包在 `.artifacts/ui-refinement-2026-09-12/Rill.app`，使用本机现有 Apple Development 身份签名；未覆盖已安装应用。
- 已走通：活动结果前置、执行详情展开／收起；工作流未保存修改提示、取消保留草稿、放弃临时草稿后跳转；搜索设置分区及自动展开；中英文切换后恢复中文；记录详情、文本编辑取消、清除筛选。
- 发现并修复：双栏工作流标题受操作按钮挤压；记录更多菜单过宽；筛选无结果时旧记录详情仍显示。后者新增行为回归测试，清除筛选后保留原选择。
- 原生测试服务截图的内容区可以辅助检查，但 AppKit 侧栏选择态仍有离屏渲染缺失，不能作为完整视觉验收。
- Computer Use 的拖动／滚动操作重复返回 `noWindowsAvailable`，普通键盘输入也未生效；AX 按钮、字段赋值、搜索深链和窗口截图可用。窗口实际缩放、Fn 实录、多屏边缘和完整 VoiceOver 操作尚未验证，不计为通过。

## 覆盖与尚未完成的验收

| 表面 | 实现范围 | 验收证据与限制 |
|---|---|---|
| 活动／历史 | 待处理优先、最终结果优先、过程折叠、真实回执、隐私受限预览、失败入口 | 实际窗口查看和展开；隐私与历史行为测试通过 |
| 记录／路由 | 700pt 紧凑导航、过滤后详情、目标名称、单一编辑入口、错误保留、危险操作区分 | 真实记录浏览、取消编辑及过滤；选择与目标文案测试通过；浮窗直贴未实测 |
| 工作流／词汇 | 860pt 紧凑导航、底部保存、草稿保护、内置只读、词条与操作对齐 | 草稿取消／放弃交互已实测；断点和导航测试通过；保存失败真实环境注入未做 |
| 设置／搜索／诊断 | 状态摘要、深链、部分搜索失败不阻断结果、详情折叠 | 中英文、搜索设置跳转实测；既有设置、搜索和焦点测试通过 |
| 字幕／菜单栏／弹层 | 稳定计时宽度、动作命中区、标签与提示、滚动内容和固定动作 | 源码与行为测试；真实 Fn 录音、多屏、完整 VoiceOver 尚未验收 |
| 跨环境 | 系统风格、现有圆角、对比度选择态、减少动态效果、双语 | 真实浅色窗口与中英文已查看；深色仅辅助渲染；精确窗口尺寸及辅助功能矩阵尚未实测 |

- Computer Use 阻塞影响的剩余工作：960×720／1280×800／最小窗口真实缩放，699/700 和 859/860 的实际界面边界，Fn 松开生命周期，数字直贴与目标应用切换，多屏边缘裁切，VoiceOver 和系统辅助功能矩阵。
- 未验证项不能计为全范围验收完成。没有覆盖安装、提交、合并或发布。
- 基线差异、应用与截图保存在仓库忽略的 `.artifacts/ui-refinement-2026-09-12/`；截图含个人历史，不进入版本库。

## 最终构建复核

- 全量 CI 后再次构建最终 Release（155.83 秒），重新组装并验证本地开发签名。运行包路径仍为上述独立 QA 包。
- 最终包已确认工作流标题布局、内置表单及保存按钮只读、筛选零结果隐藏旧详情、清除筛选恢复原选择。
- 最终截图：`after/activity-final.png`、`workflows-final.png`、`workflows-readonly-final.png`、`records-final.png`、`records-filter-empty-final.png`、`settings-final.png`，均相对于本地 artifact 根目录。
- artifact 中的 `source-sha256.json` 记录最终 Sources、Tests 和脚本哈希；`final.patch` 与 `logs/` 保存工作区差异及检查输出。原 37 个基线修改文件均仍在最终差异中，Core／Runtime 和持久化源码没有本轮变更。
